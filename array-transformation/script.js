let array = [];

// Define function to parse array
function parseArray() {

// Define an empty array
  array = [];
// place the text of the input into a variable (string)
  let text = document.getElementById("array").value;
  console.log(text);
// Read until a comma
// push everything that has been read into the array
// Repeat
// Console.log the array
}